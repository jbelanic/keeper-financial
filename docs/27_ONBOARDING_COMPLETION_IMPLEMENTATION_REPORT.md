# Minimum Onboarding-Completion Implementation Checkpoint

- **Date:** 2026-07-20
- **Branch:** `feat/onboarding-completion`
- **Starting commit:** `87796f16f9ce533c672b621c0e5d9daf4054e2f0`
- **Alembic head:** `20260719_0008` (no migration added)
- **Status:** owner-accepted bounded source implementation; controlled draft-PR publication authorized

## Implemented boundary

Keeper can issue one configured owner-maintained Documenso ICA template to the exact active assignment's `Candidate.user_id` user, persist only a provider document/signing URL after exact template preflight plus documented-response ID/source/external-ID/recipient validation, and retain provider-authoritative refresh and replacement history. Manual/recovery links cannot satisfy readiness or completion; rejected, voided, or recovery-only current envelopes can be superseded only after a fully validated Keeper reissuance succeeds. One explicit administrator/AAL2 operation revalidates the exact lifecycle and atomically/idempotently completes the ready assignment, activates its application and candidate relationship, retains candidate access, grants the existing `agent` role once, and appends safe history/audit evidence. Completed history remains candidate/admin-readable and the user becomes eligible for existing agent-profile administration.

No webhook, agreement authoring/text/file storage, arbitrary template/recipient/email content, embedded signing, automatic completion, automatic profile creation/publication, deployment, shared-database mutation, credential/external-service change, or broad Phase 1F work was added.

## Evidence and remaining ceremony

Focused adapter, onboarding/API, agent-eligibility, authorization, rollback, lifecycle-negative, container-routing, and web component tests passed with synthetic evidence. Independent review found and corrected candidate predecessor/non-actionable signing-link exposure, a manual-envelope completion-provenance bypass, conditional completion AAL2, withdrawn/stale lifecycle activation, stale administrator recipient projection, incomplete Documenso issuance-schema validation, provider-token signing-link compatibility, and a safe-reissuance recovery dead end. Final pre-commit review then identified that issuance and refresh required undocumented `envelopeId`, response `templateId`, and recipient provenance fields; official Documenso v2 documentation instead identifies documents by top-level `id`. The adapter and fail-closed fixtures were corrected to the documented template preflight, issuance response, and refresh schemas before publication. Focused re-review then required rejection of additional template recipients before distribution and rejection of an empty `/sign/` path; both guards and regression tests were added before publication. Subsequent local-stack validation found and corrected three operational defects: server rendering now selects internal API and Supabase routes while browsers retain loopback URLs; the careers page is rendered dynamically so a build-time API failure cannot become a frozen unavailable page; and migration commands rebuild the current API image before invoking Alembic. The complete API test directory passed **390 tests** with **11** environment-gated skips. Root web tests passed **30 files** with one integration file skipped, **150 tests passed** and **3 skipped**. Ruff check/format, mypy (56 source files), workspace ESLint/type/format checks, `npm audit --audit-level=high` (zero vulnerabilities), `pip check`, and the Next.js production build passed. A real isolated PostgreSQL race completed two simultaneous requests with one role grant, one completion audit, and exactly the application/relationship history pair. The repository's isolated PostgreSQL fresh-upgrade test reached a clean Alembic head, direct and live-container inspection report the single `20260719_0008` head, Compose configuration passed, and the rebuilt Docker-backed `make migrate-check` returned `No new upgrade operations detected.` Two consecutive OpenAPI/contract generations produced stable SHA-256 hashes (`c510ae6c52ba79f9ab0edb8488ad4f95ac7bbcecfb50e00bbc9375051e5bb83b` and `fbab3073d59b75cfcc596b8fbd22761385cfbdef8acd3b5e8d903d0f7b0c7418`). `git diff --check` passed.

Live local browser validation rendered the synthetic published opportunity at `/careers`, routed the administrator boundary to `/auth/sign-in?returnTo=/admin`, produced no browser-console errors, and recorded the web container's internal recruitment request as HTTP 200 in API logs. Administrator credentials were not entered, so successful credential and AAL2 completion remains owner-run evidence. Gitleaks, Trivy, Bandit, and Semgrep were not installed; no claim is made for those scans.

No live Documenso ceremony was run. The remaining live-version compatibility ceremony is exactly: configure approved template/signer IDs; send one synthetic agreement; open the signing link; exercise failed/recovery-only safe reissuance; sign the current replacement; administrator refresh to completed; complete onboarding; verify the user in the eligible agent-profile selector.
